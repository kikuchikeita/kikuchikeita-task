<p id="notice"><%= notice %></p>

<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, task_path(task) %> | <%= link_to "削除", task, method: :delete, data: { confirm: 'Are you sure?' } %> 
  </li>
  <% end %>
</ul>
<br>

<hr>
<h2>タスク登録</h2>

<%= render 'form', task: @task %>

